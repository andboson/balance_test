##TODO

```
    create database and edit config/app.yml

    cd $GOPATH
    
    mkdir -p src/app && cd src/app
    
    git clone REPOPATH  .
    
    glide install
    
    go test -v $(glide novendor)
        
```

